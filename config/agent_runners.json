{
  "planner": {
    "runner": "claude",
    "commandTemplate": "claude --permission-mode bypassPermissions --print {prompt_file}"
  },
  "developer": {
    "runner": "claude",
    "commandTemplate": "claude --permission-mode bypassPermissions --print {prompt_file}"
  },
  "ops": {
    "runner": "codex",
    "commandTemplate": "codex exec --full-auto {prompt_file}"
  }
}
