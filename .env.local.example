# 本地开发环境配置（Windows / Linux 本机）
# 复制此文件为 .env.local 并填写实际值
# 访问: http://localhost:3000（前端） http://localhost:8000（后端）

TRADING_MODE=testnet

BINANCE_API_KEY=your_testnet_api_key
BINANCE_API_SECRET=your_testnet_api_secret

LLM_PROVIDER=claude
LLM_API_KEY=your_anthropic_api_key
LLM_MODEL=claude-opus-4-6
LLM_TIMEOUT_SECONDS=30

# docker-compose.local.yml 会覆盖这两项，这里留作参考
DATABASE_URL=postgresql://alphapilot:alphapilot@localhost:5432/alphapilot_dev
REDIS_URL=redis://localhost:6379/0

MAX_POSITION_SIZE_PCT=0.20
MAX_DAILY_LOSS_PCT=0.03
MAX_CONSECUTIVE_LOSSES=3
MAX_SINGLE_RISK_PCT=0.01

STRATEGY_LOOP_INTERVAL_MINUTES=15
POSITION_MONITOR_INTERVAL_SECONDS=10
