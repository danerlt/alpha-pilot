## Durable notes

- AlphaPilot Phase 3 has moved from "to build" to active closeout/stabilization: backend tests, Alembic migration chain, deployment consistency, dashboard safeguards, and runtime config center were prioritized.
- Added project convention to record engineering progress in `docs/worklog/` with date/topic files, including what changed, why, verification steps, and commit ids.
- Added project convention that after each verified implementation chunk: run tests/build, commit, push, then auto-deploy dev server with `bash scripts/deploy-dev.sh`.
- Added project convention to never read real env files (`.env`, `.env.local`, `.env.dev-server`, `.env.test`, `.env.prod`); only example/template env files may be read.
- Implemented encrypted runtime config foundation backed by DB (`system_settings` + Alembic migration) for Binance testnet/mainnet credentials, trading mode, some LLM settings, and some risk parameters; runtime settings now support DB overrides plus in-process refresh.
- Added runtime config API endpoints (`GET/POST /api/config/runtime`) and a frontend dashboard config panel for switching testnet/mainnet and storing API credentials without re-showing secret plaintext.
- Improved runtime config UI for mobile by switching to summary cards + single active edit section, and added extra confirmation when switching to mainnet.
- Recovered dev backend deployment by making `backend/scripts/upgrade_db.py` detect legacy databases with existing tables but empty `alembic_version`, stamp revision `20260316_0001`, then upgrade to head; this fixed backend restart loop on dev server.
- Current verified backend test baseline reached 64 passed after adding config/migration/runtime-config API regression coverage.
- Dev environment was successfully redeployed after backend recovery and subsequent runtime-config/mobile-UI changes; public dev URLs are under `/ap-dev`.
- New standing rule from user: whenever the user proposes a new requirement/feature, it must also be written into `docs/产品需求文档.md` (not just worklog/plan/spec).
- New standing rule from user: both Claude-side and Codex-side development workflows should use the superpowers methodology/skills, and this expectation should be treated as persistent project guidance.
- Verified from live OpenClaw gateway config that Telegram bot display names are: `AlphaPilot Bot`, `AlphaPilot Planner Bot`, `AlphaPilot Developer Bot`, `AlphaPilot Ops Bot`; when referring to bots in group coordination, use these real configured names instead of invented/tencent-style names.
- User supplied the real Telegram @handles for group coordination: planner=`@alpha_pilot_planner_bot`, developer=`@alpha_pilot_developer_bot`, ops=`@alpha_pilot_ops_bot`.
- Updated group-chat coordination rule from user: in group messages, do NOT use real @mentions and do NOT use full bot display names; instead, refer to subagents plainly as `planner`, `developer`, `ops` for human-readable task assignment/status updates.
- Finalized group-chat dispatch convention from user: I may write `@planner` / `@developer` / `@ops` in group messages as display-only formatting, but actual task assignment must happen via direct backend agent interaction/session control, not by relying on group-message mentions to trigger subagents.
