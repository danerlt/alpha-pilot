"""Exchange adapter subpackage.

Business code depends only on ExchangeAdapter + value types; concrete
implementations (BinanceAdapter in Task 16, future MockAdapter or
HyperliquidAdapter) are injected via constructor.
"""
