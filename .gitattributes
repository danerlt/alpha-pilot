# 强制 Linux 服务器侧执行的文件用 LF 行尾，防 Windows 开发机的 CRLF 导致
# `bad interpreter: /bin/bash^M` 等部署故障（无论服务器 git autocrlf 如何配置）。
*.sh        text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.sql       text eol=lf
Dockerfile* text eol=lf
