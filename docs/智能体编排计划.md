# Plan: Agent Orchestration for AlphaPilot

## Phase 1: Define docs and artifacts

### Step 1.1
写入 orchestration spec，明确 `planner -> developer -> ops` 的主干流程

### Step 1.2
写入 orchestration plan，明确 planner 先分析需求并更新 docs

### Step 1.3
写入 worklog，记录本次设计收敛过程

### Step 1.4
补齐以下设计文档：
- architecture
- context model
- run artifacts
- handoff schema
- input whitelist
- broadcast protocol
- ops substages
- personas
- message templates
- product rules

## Phase 1.5: Lock execution contract

### Step 1.5.1
将 planner 的交付物固定为：需求分析、范围界定、方案文档、docs 更新、developer handoff

### Step 1.5.2
将 developer 的交付物固定为：功能代码、白盒测试、单元测试、实现说明

### Step 1.5.3
将 ops 的交付物固定为：验收结论、测试复核、部署记录、上线总结

## Phase 2: Add local scaffolding

### Step 2.1
补四角色目录：
- agents/alpha-pilot
- agents/planner
- agents/developer
- agents/ops

### Step 2.2
写入每个角色的：
- AGENTS.md
- SOUL.md
- IDENTITY.md

### Step 2.3
补 feature / bug workflow 草案

### Step 2.4
补 run template 目录结构

## Phase 3: Align with real execution layer

### Step 3.1
确认 antfarm 是否已支持每 agent 指定执行器（Claude/Codex）

### Step 3.2
如果未支持，补充执行层扩展设计：
- planner -> Claude
- developer -> Claude
- ops -> Codex

### Step 3.3
定义 alpha-pilot 创建 run、写入 artifacts、驱动 workflow 的入口

## Phase 4: Telegram integration (later)

### Step 4.1
等待用户提供：
- bot token
- bot username
- accountId
- 群 chat id

### Step 4.2
补 telegram account routing 与群播报接线

## Verification

1. docs 中形成完整、可读、可执行的设计链路
2. 本地 scaffolding 与真实 AlphaPilot 仓库结构兼容
3. 不破坏现有 superpowers workflow 约束
