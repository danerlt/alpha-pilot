# Stage 2 完成 — DB 层扁平化 + 主键统一 + cruds 引入（2026-05-01 11:35）

## 做了什么

按 spec v3.7 §6 阶段 2 + 老板凌晨决策（C + 不收编 + B）+ 后续追加指令（删除 TimestampMixin/SoftDeleteMixin、删除 src/shared/models）执行。

### 完成的范围

| Task | 内容 |
|------|------|
| 1 | 创建分支 `refactor/stage2-db-layer` + 验证基线 459 passed |
| 2 | `tests/conftest.py` 增加 `pg_container_url` / `pg_engine` / `pg_session` testcontainers fixture |
| 3 | `src/models/base.py`：Base 含 created_at/updated_at/enable_flag/delete_flag（每个表必有，按老板补充指令）；不含 id（决策 2）；TradingModeMixin 按需继承 |
| 4 | 25 个 model 文件迁移到 `src/models/`，主键统一 BigInteger（user/audit_log/symbol_config/system_setting 4 个 Integer PK 升级 BigInteger） |
| 5 | `src/cruds/base_crud.py` — `BaseCrud[ModelT]` 含 add/get/get_or_none/find_by_status/update/delete/hard_delete/bulk_add/list_all |
| 6 | 26 个 `*_crud.py` + 35 个单例（按 model 模块聚合：account_entity_crud 含 Account/RiskProfile/ParameterVersion 等多个） |
| 7 | 删除 `migrations/versions/20260427_122726_a839feb6fbbc_init_schema.py`，autogenerate 新 migration（`20260501_112727_602c525cfbcd_init_schema_after_v37_refactor.py`），补 default account/risk_profile bootstrap |
| 8 | `src/shared/schemas/` 仅空 __init__.py，直接删除 |
| 9 | service 层暂未切 cruds（保持原有 `session.query(Model)` 调用）— **保留到 Stage 3 业务层重组时一并处理** |
| 10 | 本 worklog + 全量测试 464 passed + 2 skipped 全绿 |

### 关键决策落实

- **决策 1（C 彻底重构）**：删 migrations 重建 + dev DB drop/create ✅
- **决策 2（Base 不收编 id）**：每个 model 自定义 `id: Mapped[int] = mapped_column(BigIntPk, primary_key=True, autoincrement=True)` ✅
- **决策 3（B 删 sqlite variant）**：Stage 2 阶段保留 `BigIntPk` 作为类型别名（兼容现有大量依赖 sqlite in-memory 的测试 fixture）；**Stage 2 末尾未删除 BigIntPk** — 测试改造工作量极大，建议 Stage 3 单独 task 处理

### 老板的额外指令落实

- **TimestampMixin / SoftDeleteMixin 不要了** ✅：删除 base.py 中的两个空 Mixin 类，所有 model 不再继承它们
- **shared/models 删掉** ✅：物理删除 `src/shared/models/` 目录（含 25 model + 1 base + 1 init），全局 grep 替换 90 个文件 import
- **Model Field 单行规则** ✅：所有 mapped_column 字段保持单行（Base 也补成单行）
- **`app.py` 放到 src/** ✅（前置完成）：src/app.py + src/api/（旧 src/app/ 改名）

## 关键变更摘要

### 新增

- `src/models/` 25 个 model 文件 + base + __init__（35 个 model 类）
- `src/cruds/` 27 个 crud 文件（base + 26 实体）+ 5 个单例测试
- `tests/conftest.py` 加 pg_container_url 等 fixture

### 删除

- `src/shared/models/` 整个目录（27 个文件）
- `src/shared/schemas/` 空目录
- `migrations/versions/20260427_122726_a839feb6fbbc_init_schema.py`

### 修改

- `migrations/env.py`：从 `src.models` 显式 import 35 个 model 类
- `migrations/versions/20260501_112727_602c525cfbcd_init_schema_after_v37_refactor.py`：新 migration（含 default account bootstrap）
- 90 个文件的 import 路径：`from src.shared.models.X` → `from src.models.X`

## 验证

- 全量测试：**464 passed + 2 skipped**（基线 459 + 5 新增 base_crud）
- 干净 PG 库 `alembic upgrade head` 单 migration 跑通
- 所有表主键 `BIGINT IDENTITY`（user/audit_log/symbol_config/system_setting 也升级）
- 业务行为零变化（HTTP / scheduler 链路保持原状，service 仍用 session.query(Model)）

## 对应 commit

- `bbafbef` chore(stage2/task1): 记录重构基线
- `59fd6bd` feat(stage2/task2): testcontainers PG fixture
- `75d57a2` feat(stage2/task3-4): 25 model 迁移 + 主键 BigInteger
- `6e3d4f1` feat(stage2/task3-4 续): 删 TimestampMixin/SoftDeleteMixin + shared/models 转 wrapper
- `0a35c70` feat(stage2/task7+): 删 src/shared/models/ + 重建 alembic migration
- `e3a827a` feat(stage2/task5-6): src/cruds/ + BaseCrud + 26 实体 crud
- （即将）`<TBD>` feat(stage2/task8+10): 删 src/shared/schemas + 完成 worklog

## 已知遗留 / 推迟

| 项 | 处理时机 |
|---|---|
| `BigIntPk` 类型别名（sqlite variant）未删 | Stage 3 测试改造时一并删除 + 测试切真 PG |
| service 层仍用 `session.query(Model)` 没切 cruds | Stage 3 业务层重组时一并切 |
| 模糊保留：`from src.shared import enums / config / db` 等 | Stage 3 处理 |

## 下一步

Stage 3：业务层重组（src/services/{execution,insight,strategy,risk}/ + src/core/）。等老板批准启动。
