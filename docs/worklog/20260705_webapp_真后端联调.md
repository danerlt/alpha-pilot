# 2026-07-05 · webapp ⇄ 真后端联调（wire 适配层）

## 做了什么

后端 P1/P2/B1-B3 落地后，前端完成真联调三件事（本会话只动前端，后端缺口写档移交）：

1. **wire 适配层**（`src/api/wire.ts`）：OpenAPI 重导（43 paths）+ `gen:api` 后，把真实
   snake_case 契约全量映射到前端领域类型；MSW mock 同步 wire 化（`mocks/wireMocks.ts`），
   保证 mock 与真实后端**同构**——适配链路被既有 15 个测试全覆盖，改完全绿。
2. **联调模式**：`npm run dev:real`（vite `--mode real`，5174 端口，`/api` `/ws` 同源代理），
   cookie 直通、零 CORS、自动禁用 MSW。
3. **浏览器级实测**：隔离库 `alphapilot_webapp_it` + 纯环境变量注入管理员（零后端代码/配置
   改动），跑通 登录（`ap_token` httpOnly cookie）→ 2FA → 主控台/行情/决策流/持仓 真数据渲染，
   控制台零错误；唯一 404 是已知缺口 `/api/account/history`。

## 发现的后端缺口（已移交）

7 项契约缺口 + 复现环境写入 [`docs/webapp联调-后端待办.md`](../webapp联调-后端待办.md)，
并插入后端会话锚点记忆（`.claude/memory/project_handoff_backend_progress.md`）待做清单首位。
高优先三项：DecisionRead 补守卫裁决字段、`GET /api/account/history`、`GET /api/orders` 列表。

## 如何验证

- `npm run build` + `npm run test`（15 用例）全绿
- mock 模式（5173）回归：wire 往返后页面与之前一致（决策卡/权益/事件流/持仓）
- real 模式（5174）：真实登录链路 + 四页真数据 + failed 网络面仅已知缺口

## 对应 commit

- `68c9109` 真后端联调 — wire 适配层 + 浏览器级全链路打通
