# Windows 本地全流程差距收口（2026-06-13）

## TL;DR

老板要求"Windows 本地全自动全流程完成项目"。以多智能体差距审计为起点，对 spec / 计划 / 代码做全面对账，发现 48 个差距项，逐项亲核 + 实现 + 验证。

**最终：后端 474 passed + 2 skipped（较起点 +21 测试），前端 `next build` + `tsc --noEmit` 通过，新增代码 ruff 全清，真实启动后端跑通 12 项端到端冒烟。**

## 起点状态

- 后端基线 443 passed + 2 skipped；但本机首跑全 ERROR（DB 容器停了 4 周）。
- 前端 `npm install` 失败（package-lock.json 锁的是腾讯云内网镜像 URL，本机不可达）。
- `docker/` 目录被 Makefile 引用但 Dockerfile.backend COPY 已迁走的路径。

## 差距审计（多智能体工作流）

6 维度并行审计（后端验收 / 前端验收 / Phase G 文档 / task_dispatcher 接入 / OPS 一致性 / 测试质量），产出 48 个带文件级证据的差距项。复核阶段当时撞会话限额未跑完，故逐项实现前均亲自核实代码再动手——其中发现审计的 OPS-7/8/11 在 rebase 进来的 `1ff5808` 中已被修正（审计跑的是会话启动时的旧 CLAUDE.md）。

## 实现分块（每块 commit + push 到 dev）

### Block 1 — 基础设施（`ffabd32`）
- `Dockerfile.backend` 删除已迁走的 `migrations/` + `alembic.ini` COPY（修 docker build）
- `Makefile` 新增 `deps-up/deps-down`（pytest 依赖的 PG 5442 + Redis 6389 栈此前无 target）；本地 venv 目标 `source .venv/bin/activate` → `uv run`（Windows/Linux 通用）
- `conftest.py` alembic 子进程 `uv run` → `sys.executable -m alembic`（不再要求 PATH 有 uv）
- 前端 `package-lock.json` 重新生成（腾讯云内网镜像 → npmmirror）；加 `npm run typecheck`

### Block 2 — 异步任务闭环（`95c90d9`，spec §4.9.1 真·剩余项）
- `POST /api/commands/close-all` 同步执行 → `task_dispatcher.enqueue`，返回 `{task_id, status: queued}`
- 新增 `GET /api/tasks/{task_id}`（admin-only，HTTP 兜底层）
- 终态 SUCCESS/FAILED 写 `task.status_changed` outbox 事件 → EventShuttle → WS 实时层
- `recover_orphans` 补 PENDING 超时孤儿重推（LPUSH 丢失场景；从未执行不算重试，CAS 防重复消费）
- `project.md` §8 标注 v1 实施偏差（状态机大写命名/无 cancelled/无 v1 前缀）

### Block 3 — 后端 admin spec 缺口（`ed400db`，BE-1~6）
- `POST /api/admin/users` admin 建号入口（公开注册按 C5 禁用后唯一运行时建号途径）；仅存 hash + 查重 + 写审计
- runtime config 更新写审计（Security Constraints #4），敏感 key 值脱敏 `***`
- audit-logs 响应补 `actor` 字段（user_id→username 映射）
- admin 三处审计写入填充 `ip/user_agent`（此前恒 null），新增 `client_meta()` 依赖

### Block 4 — 前端（`6a21062`，FE-1~9）
- 主 dashboard 裸 `fetchJson/postJson`（不解 envelope/不带 token）→ 统一走 `lib/api`，修复登录用户数据全 401 吞空
- 平仓/全平改调 `/commands/*`（旧 `/positions/{id}/close`、`/positions/close-all` 是 404）；一键平仓适配异步契约
- 平仓/一键平仓/解除熔断控件按 `isAdmin` 收口
- AI 决策日志区补移动卡片回退（≤768px 不再空白）
- 未登录首页移除指向已禁用注册页的 CTA；审计页 actor 显示用户名
- 删除孤儿 `/preview/dashboard` + 仅其使用的 `components/ds` 第二套设计系统（消除双壳）

### Block 5 — 文档（`3807311`，DOC-1~7）
- 新建 `docs/总体架构.md`（四平面/数据流/运行时/演进路线/硬风控约束）
- `认证与管理后台计划.md` Phase E/F/G 全部勾选同步
- PRD 章节编号修正（消除两个 #9 / 重复 ### 8.2.3 / §9 下错用 8.x，全文唯一连续）
- `CLAUDE.md` 测试数 443→453、task_dispatcher 状态、env 密钥示例、`make deps-up`

### Block 6 — 测试/质量（`aa86f8d`，TEST-1/2/4/5/6/8）
- 删死代码 `decision_engine.py` + `decision_prompt.py`（0 外部引用）
- `ruff check --fix` 清 210 项安全错（I001 import 排序 + F401 未用导入），全仓规范化；新增文件 E501 全修
- `pyproject` filterwarnings 治理 4 个第三方 DeprecationWarning
- 新增 21 个测试：`test_ws_manager.py`(3) + `test_reporter.py`(3) + `test_read_endpoints.py`(15)

## 端到端冒烟（真实启动后端 8099 + smoke DB）

12 项全过，对应今晚管理后台计划 7 条验收标准：
1. 公开注册禁用（400004）/ 错密码拒绝（400003）
2. admin 登录拿 token + `/me`
3. admin 建普通用户 bob（role=user）
4. bob 越权访问 admin → 400004（守卫生效）
5. admin 改 bob 角色 → admin
6. admin 建币种配置（btcusdt→BTCUSDT 归一）
7. **审计日志 actor=admin（用户名映射）+ ip=127.0.0.1（填充）** — 端到端验证 BE-5/6 + FE-8
8. close-all 异步入队 → task_id=1/queued；GET /api/tasks/1 → PENDING（无 scheduler 消费，正确）
9. runtime-config 更新写审计，after_json 敏感值脱敏

全程无 500 / 未处理异常。

## 残留（明确不在本轮自动会话内处理）

- **pre-existing ruff E501（~300 行长）/ E702（~40 测试分号）**：全仓 pre-existing 风格债，跨整个代码库；大批改写属项目哲学明确警告的"大爆炸"高风险改动且纯风格非正确性。本轮只保证新增代码 ruff 全清，残留留待专项格式化 PR（可 `make fmt` 全仓跑一次，需老板单独 review 大 diff）。
- **TEST-7 前端单元测试框架**（Jest/Vitest）：V0.1 无前端测试基建，本轮以 `tsc --noEmit` + `next build` 作为前端门禁，单测框架引入留待 V0.2。
- **dev 环境 24h 观察**：策略链/熔断/调度长稳需老板亲自跑。
- **浏览器 UI 冒烟**（移动端布局/危险操作二次确认实际点击）：今晚计划标注"老板亲自验收"，本轮以 build 编译 + API 契约验证替代。

## 验证汇总

| 项 | 结果 |
|---|---|
| 后端全量 pytest | 474 passed + 2 skipped，0 warning |
| 前端 `next build` | 通过（/preview 路由已移除） |
| 前端 `tsc --noEmit` | 通过 |
| 新增代码 ruff | All checks passed |
| docker build backend | 成功（OPS-1 实证） |
| 端到端冒烟 | 12/12 通过，无 500 |
