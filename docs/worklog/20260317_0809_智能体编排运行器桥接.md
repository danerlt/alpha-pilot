# 2026-03-17 Agent Orchestration Runner Bridge

## 做了什么

1. 明确了角色执行器桥接文档：
   - planner -> Claude
   - developer -> Claude
   - ops -> Codex
2. 新增 `config/agent_runners.json`
3. 新增 `scripts/run_agent_stage.py`
4. 为三个角色生成 prompt 文件和建议执行命令，完成一轮本地 dry-run

## 为什么做

这是从“文件状态流”继续往“真实角色执行”推进的一步。
当前先不直接执行 Claude/Codex CLI，而是先把 prompt 构建与执行器映射固化，降低后续接线复杂度。

## 当前结果

AlphaPilot 现在已经具备：
- 角色 -> 执行器 映射
- prompt 文件生成
- 建议执行命令渲染
- 继续往真实 CLI 调用扩展的基础
