# 20260706 handoff 后端 P5 + P6 收官 — 路线图后端部分全量完成

## P5 · 策略实验室（handoff 3.5）

- **新表 `lab_candidates`**（迁移 `20260706_100620`）：受控进化流水线
  QUEUED → SHADOW → CANARY → LIVE（terminate→RETIRED；灰度回撤→ROLLED_BACK）
- **6 个端点**（`/api/lab/...`，权限走 P4 矩阵：查看=lab.view 全角色 / 提交·启动·终止=trader+ /
  **批准灰度·上线=admin+**）：candidates 列表（含影子进度、影子 vs 线上对比、promote 门槛状态）/
  提交 / start / promote / terminate / history 时间线（复用 audit_logs，自动回滚标 system）
- **promote 门槛服务端校验**（前端绕不过）：影子期进度 ≥60% 且影子样本 ≥3 且影子模拟收益优于线上基线
- **影子执行 V1 = mirror 模式**（`services/lab/shadow_runner.py` + `lab_scanner` 调度 job，与策略循环同频）：
  对 SHADOW 候选镜像主管道最新真实决策，应用 params_json 变换（sl_mult/tp_mult/size_scale/min_confidence）
  生成影子 proposal 写 shadow_decisions（**不下单**），评估轮按当前价模拟 PnL 写 shadow_evaluations 逐笔对比；
  幂等（同 run 同真实决策只镜像一次）。**真·独立 LLM 决策链留 V2**（每候选独立 prompt→solve→review）。
- **灰度自动回滚**：CANARY 候选在日亏达熔断上限 80% 时自动 ROLLED_BACK（比全账户熔断更早叫停新策略）
  + `lab.update` 事件（新事件契约，入注册表）+ 审计（operator=system）

## P6 · 绩效归因（handoff 3.8 + 联调缺口#5）

- `GET /api/performance/summary?range=`：净收益 vs HODL（BTCUSDT 收盘价归一化基准）/ Sharpe / Sortino
  （权益曲线日收益率年化 √365，样本不足返 null）/ 最大回撤 / 胜率 / 盈亏比 + 对比曲线（下采样 ≤200 点）
  + **缺口#5 磁贴指标**（today_trades / avg_holding_seconds / week_pnl / month_pnl）
- `GET /api/performance/monthly`：月度 PnL 桶（零轴柱图数据源）
- `GET /api/performance/attribution?dim=symbol|strategy|trigger`：净收益降序归因分解
- **对账测试**（roadmap P6 验收）：seed 已知交易断言 win_rate/盈亏比/月度桶/归因合计与 DB 记录精确一致

## 联调待办复查（老板指令）

`docs/webapp联调-后端待办.md` **#1-7 全部收口** ✅（含 #1 顺带项：决策列表直出
entry/sl/tp/size 四字段），清单已回写状态；「已在后端计划内」三项（P2b/P4/P6）随本批全部落地。
openapi.json 66 paths，前端 `npm run gen:api` 后可删全部兼容代码，绩效页可全量接真。

## 验证

- 全量回归 **688 passed + 2 skipped**（P4 后基线 668），ruff 全绿
- 关键用例：promote 门槛拦截 / mirror 变换与模拟 PnL 纯函数 / 影子幂等 / 灰度回撤自动回滚（含 system 时间线）/
  lab RBAC（viewer 只读、trader 不能 promote）/ 绩效对账

## commits

- `c649090` feat(lab): 策略实验室 — 候选生命周期/影子mirror执行/promote门槛/灰度自动回滚（P5）
- `67df944` feat(performance): 绩效三接口 + 缺口#5 磁贴指标（P6）
- 本条 worklog 随缺口#1 顺带字段一起提交

## 里程碑

**handoff 路线图 P1-P6 后端部分全部完成**（P0 为纯前端）。累计新增端点 30+，
测试基线从开工时 526 → **688 passed**。

## 遗留（非阻塞）

- 老板本机 dev 库欠 `make upgrade-db`（累计 4 个迁移）
- 影子执行 V2（真·独立 LLM 决策链）/ agent 真流式 / 通知渠道 token settings 化 + notifier 接线
- roadmap 各期的"testnet 实盘验收"项（真实决策链逐段点亮、手动单全链路、影子跑 24h）需前后端联调 + 老板亲验
