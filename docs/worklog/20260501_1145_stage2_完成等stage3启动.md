# Stage 2 完成 — 等老板批准 Stage 3 启动（2026-05-01 11:45）

## TL;DR

✅ **Stage 2 完成并合并 main**：DB 层扁平化 + 主键统一 + cruds 引入 + migrations 重建 + dev DB 重建。
**测试 464 passed + 2 skipped**（基线 459 + 5 新增 base_crud），全绿。

## 已完成的范围（Stage 1 + Stage 2 累计）

### Stage 1（基础设施层，已合 main）
- `src/utils/{log,request_id,uuid,time,redis,json}.py`
- `src/configs/app_configs.py` 8 子配置类多继承
- `src/db/{engines,session}.py`
- `src/common/{response,exception,api_response,events,schemas}/`
- `src/middleware/{request_logging,error_logging}/`
- `src/app.py` 提级 + `src/api/`（旧 `src/app/` 改名）
- 中间件栈注入 + 全局 exception handler 注册

### Stage 2（DB 层扁平化，已合 main）
- `src/models/` 35 个 model 类（迁自 src/shared/models/，主键统一 BigInteger）
- `src/cruds/` BaseCrud + 26 个实体 crud + 35 个单例
- `migrations/versions/` 重建：单 migration `init_schema_after_v37_refactor`
- `src/shared/models/` 整个目录删除（含全局 90 文件 import 替换）
- testcontainers PG fixture（`pg_container_url` / `pg_engine` / `pg_session`）
- Base 含 `created_at` / `updated_at` / `enable_flag` / `delete_flag`（每表必有）
- 删除 `TimestampMixin` / `SoftDeleteMixin`（内容已在 Base，老板指令）
- 4 个 Integer 主键表升级 BigInteger（user / audit_log / symbol_config / system_setting）

## 老板凌晨决策落实状态

| 决策 | 落实 |
|------|------|
| 1. C 彻底重构（删 migrations 重建） | ✅ 完成 |
| 2. Base 不收编 id 字段 | ✅ 完成 |
| 3. B 删 sqlite variant | ⚠️ 部分推迟 — `BigIntPk` 别名暂保留（依赖大量 sqlite in-memory 测试 fixture），Stage 3 测试改造时再删 |

## 老板的额外指令落实

| 指令 | 落实 |
|------|------|
| Base 含 created_at/updated_at/enable_flag/delete_flag | ✅ |
| TimestampMixin / SoftDeleteMixin 不要了 | ✅ |
| shared/models 删掉 | ✅ |
| Model Field 单行规则 | ✅ |
| app.py 放到 src/ | ✅（前置完成） |

## 当前 git 状态

```
main: 27aa6e5
分支：仅 main（refactor/stage1-* 和 refactor/stage2-* 分支已删）
工作区：clean
```

## 待老板决策的事项

### 1. Stage 3 启动？

Stage 3 计划：业务层重组（`src/services/{execution,insight,strategy,risk}/` + `src/core/`），以及把 service 层的 `session.query(Model)` 切到 cruds 调用。

请老板任选回复：
- **"开始 Stage 3"** → 我先展开 Stage 3 详细 plan，再按 plan 跑
- **"先 dev 跑一下 Stage 1+2 看效果"** → 老板手动 `bash scripts/deploy-dev.sh` 验证（需 envs/dev.env，老板私有）
- **"调整 spec：xxx"** → 我按调整改 spec/plan 再继续

### 2. BigIntPk 何时彻底删除？

当前 `BigIntPk = BigInteger().with_variant(Integer(), "sqlite")` 还在 src/models/base.py 里（25 个 model 主键用它）。删除后会让 100+ 个 sqlite in-memory 测试失败，需要 fixture 全切真 PG。

可选时机：
- **A. Stage 3 内顺便做**（推荐）：业务层重组时，正好需要重写很多 service 测试，一并把 sqlite 切 PG
- **B. 单独做一个 stage 2.5 任务**：纯做测试改造，不混业务层重组
- **C. 推到最后**：Stage 5 末尾再做

我推荐 A（合理整合到业务层重组的 test fixture 改造里）。

## 对应 worklog 文件

- `docs/worklog/20260501_1108_stage2_baseline.md` — Stage 2 基线
- `docs/worklog/20260501_1135_stage2_完成.md` — Stage 2 完成详情
- 本文件 — 给老板的 read me

## 给老板早起的 quick check

```bash
# 验证 Stage 1+2 测试绿
cd backend && ALPHAPILOT_SKIP_SECRET_VALIDATION=1 uv run pytest -q --tb=no | tail -3
# 期望：464 passed + 2 skipped

# 验证 dev DB schema 正确（dropdb + createdb + alembic upgrade head 已自动跑过）
docker exec alpha-pilot-postgres-1 psql -U alphapilot -d alphapilot -c "\dt" | wc -l
# 期望：~35 张表

# 验证 default account row 在
docker exec alpha-pilot-postgres-1 psql -U alphapilot -d alphapilot -c "SELECT id, name FROM accounts WHERE id=1;"
# 期望：default account 存在
```
