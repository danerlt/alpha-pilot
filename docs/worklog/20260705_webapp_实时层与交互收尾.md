# 2026-07-05 · webapp 实时层 + lightweight-charts + 交互收尾

## 做了什么

继 2026-07-03 全量页面落地后，收掉 worklog 遗留的三项：

### ① 实时事件流层（`src/api/stream.ts`）

- 统一订阅接口，覆盖 handoff/03 §3.9 全部事件面：`risk.state` / `account.snapshot` /
  `event.append` / `decision.progress` / `decision.complete` / `market.ticker`
- **mock 模式**：定时器驱动 —— 4s 账户快照+行情 tick、9s 事件流追加、50s 完整决策周期
  （progress 五段逐个 done → complete 落卡 + 配套 ai/guard 事件）
- **真后端模式**：单条 WebSocket（`VITE_WS_URL` 或由 `VITE_API_BASE_URL` 推导 + `/ws`），
  `{type, payload}` 按 topic 分发，断线指数退避重连（1s→30s 封顶）
- 接线：AppContext（顶栏胶囊/侧栏权益实时）、主控台（事件流生长/新决策置顶触发流式重放/权益曲线生长）、
  行情页（自选列表价格 + 头部大数字 AnimatedNumber 滚动）

### ② 场景模拟（HALTED 五件套可演示）

- CommandPalette 新增「场景模拟」组（仅 mock 显示）：风控正常 / WARN / HALTED
- HALTED 触发即验证五件套联动全通过：顶栏胶囊变红、熔断横幅、事件流置顶红条、
  hero 决策被拒（熔断保护 REJECT）、权益曲线变 rose
- HaltBanner「手动恢复」→ 回 OK 场景 + 事件流补「熔断已手动解除」；风控状态变化自动重置横幅关闭态

### ③ lightweight-charts + 交互收尾

- 行情 K 线由 SVG 自绘换 **lightweight-charts 5.2**（handoff/02 P3 生产方案）：
  蜡烛 + 量能直方图 + SL/TP/入场 `createPriceLine` + 现价虚线 + 十字光标 OHLC 图例；
  颜色运行时解析 `--ap-*` token 保持零硬编码；autoSize 在嵌套 grid 下测量不稳（画布 36px），
  改显式 ResizeObserver 驱动修复
- Pilot AI「应用修改」闭环：pending action → ConfirmDialog 人工确认 → 标记「已确认生效」+
  事件流补审计事件（Agent 无权直改硬风控的产品语义落地）
- 实验室候选操作接通：排队候选「立即开始」→ shadow；「申请灰度」走人工批准确认 → canary；
  「终止」确认后归档进进化历史（服务端最终校验门槛的语义写入弹窗文案）

## 如何验证

- `npm run build`（tsc + vite）全绿
- preview 动态验证：权益/事件 10s 内实时跳动✓；HALTED 五件套逐项✓；手动恢复闭环✓；
  K 线画布 594×354 正常渲染（截图确认蜡烛/量能/标线/轴标签）✓；
  Pilot AI 应用修改确认后显示「已确认生效」✓；实验室终止/归档进历史✓
- 后端零改动，pre-commit 单测门禁通过

## 对应 commit

- `af2194b` 实时事件流层 + HALTED 场景模拟联动
- `09ac58a` 行情 K 线切换 lightweight-charts
- （本次）Pilot AI 确认流 + 实验室候选操作 + worklog

## 遗留

- webapp 部署管线（docker/CI 仍指 Next.js frontend/）—— 等老板定两条前端线去留
- 真后端接入时：确认 WS 消息外层格式是否为 `{type, payload}`，不一致改 `WsStream.onmessage` 一处即可
