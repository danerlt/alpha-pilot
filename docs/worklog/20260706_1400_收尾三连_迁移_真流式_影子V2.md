# 20260706 收尾三连 — dev 库迁移补齐 + agent 真流式 + 影子执行 V2 + 验收手册

## 1. 本机 dev 库迁移补齐（老板指令）

- `make upgrade-db` 原本必炸：**alembic env.py 回退用裸 `DATABASE_URL`（默认空串）而非
  `db_uri`（按 PG_* 拼接）**——凡 .env 未显式设 DATABASE_URL 的机器都中招。已修复。
- 迁移已跑到 head=`99ca505e8d31`（risk_events.decision_id / agent_pending_actions /
  users 2FA / lab_candidates 四个欠账全补齐）。

## 2. agent chat 真流式

- `OpenAIClient.complete_stream`（stream=True 逐段 yield；可选能力，getattr 探测）
- 工具协议升级：最终回答**直接输出纯文本**（不再要求 JSON final，旧格式仍兼容）
- chat 流式路径：首字符嗅探——`{`/反引号 开头缓冲完整走工具解析；纯文本**边收边发 delta**
  （真流式，首字延迟≈LLM 首 token）；Mock/不支持流式的客户端回退 64 字符切块伪流式
- SSE 对外契约不变（tool_call → delta → done），前端零改动

## 3. 影子执行 V2（mode=llm 真·独立决策链）

- `candidate.params_json.mode="llm"`：每周期对 PIPELINE_SYMBOLS 用最新快照
  （indicator/factor/regime/账户）独立跑完整决策链 prompt→LLM→review，
  `source="shadow"` 落 ai_decisions（审计可回放）但 **决策流列表已过滤 shadow**、**绝不下单**；
  幂等（同 run×symbol 每周期一次）；无 llm client 时跳过不炸
- 改造点：`DecisionSolver.solve` / `PipelineInput` 增加 source 参数（默认 ai_trader 零行为变化）
- 成本提示：每候选每周期每 symbol 一次 LLM 调用，提交 llm 候选前注意费用
- mirror 模式（V1）保持不变，两种模式可并存

## 4. testnet 实盘验收手册

`docs/testnet验收手册.md`：P1-P6 各期验收项 + 配置分层链路验收 + 24h 稳定性观察点 +
故障速查——老板照单亲验。

## 验证

全量回归 **697 passed + 2 skipped**，ruff 全绿。

## commits

`c9101b8` fix(db) env.py db_uri → `a2d7d67` feat(agent) 真流式 → `f681540` feat(lab) 影子 V2 → 本条（手册+worklog）

## 剩余

仅 testnet 实盘验收（需老板亲验，手册已备）；通知渠道接线已在配置分层批次完成。
