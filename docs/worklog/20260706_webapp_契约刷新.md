# 2026-07-06 · webapp 契约刷新 — 66 paths 全量接真

## 做了什么

后端把联调缺口 #1-7 + P2b/P4/P5/P6 全部落地后，前端分四步吃上新契约（每步独立提交）：

| 步骤 | 内容 | commit |
|------|------|--------|
| P1 核心域 | 决策 guard_verdict 直出（删 is_fallback 近似）+ 列表读五格字段；持仓 strategy_mode/position_pct；订单簿切 `GET /api/orders`；catchup 切 CatchupOut（envelope 复用 + payload.message 优先文案）；硬风控表切 `GET /api/risk/limits` | `4e80a8b` |
| P2 绩效+2FA | performance 三接口接真（curve 绝对权益→相对首点收益率%）；主控台 4 磁贴+近7/30日、绩效页统计全换真源（缺口#5）；**2FA 真二段式**（requires_2fa+two_fa_token → `/api/auth/2fa/login`，错误清空重输） | `4e80a8b` |
| P3 行情流+实验室 | 盘口/逐笔切 `/ws/market`（一连接一 symbol，depth/trade 帧适配，断线退避；mock 定时器抖动）；实验室 LabCandidateRead/LabHistoryItemRead 接真；**tokenStore**：access_token 内存持有供 WS `?token=` | `92c0c7d` |
| P4 设置接真 | settings 三分区全接真（交易所网络/脱敏密钥/权限实测清单、LLM 配置+连通性测试+Agent 分工、通知渠道/订阅开关即改即存）；密钥输入留空=不修改语义 | 本次 |

全程 MSW 与真实契约保持 wire 同构（mock handlers 全部返回后端同款形状），适配链路被 15 个测试覆盖。

## 新发现缺口（已写入联调待办 #8）

WS 握手（`/ws`、`/ws/market`）只认 `?token=` 不读 cookie —— httpOnly 方案下前端刷新后拿不到
token，WS 实时降级直到重新登录。过渡方案 tokenStore 内存持有；根治需后端 WS 握手加 cookie 回退。

## 如何验证

- `npm run build` + `npm run test`（15 用例）全绿，×4 次
- mock 回归逐项：决策卡五格/事件流中文文案/订单簿 FILLED/风控 7 项阈值/绩效统计真源/
  盘口逐笔流动/实验室对比▲/设置三分区（测试连接→权限清单点亮、LLM 脱敏+Agent 分工、通知开关）
- 注意事项：MSW handler 变更后 preview 需**整页刷新**（HMR 不重载 worker）

## 对应 commit

`4e80a8b` / `92c0c7d` / （本次 P4 设置接真 + 本 worklog）
