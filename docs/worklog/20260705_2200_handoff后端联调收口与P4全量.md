# 20260705 handoff 后端 — 联调缺口收口 + P4 settings/RBAC/2FA 全量

## 一、webapp 联调缺口收口（前端插队工单，`docs/webapp联调-后端待办.md`）

#1-4、#6、#7 全部完成（#5 周/月 PnL/Sharpe 聚合按清单建议留 P6）：

- #1 `DecisionRead.guard_verdict`（批量反查 risk_events 最新 GUARD_* 事件）
- #2 `GET /api/account/history` 权益曲线序列（近 N 快照点，时间正序）
- #3 `GET /api/orders` 订单列表（OrderListItemRead）
- #4 `PositionRead.strategy_mode`（决策反查）+ `position_pct`（市值/总权益）
- #6 catchup 挂 `CatchupOut` response_model（前端可删形状嗅探）
- #7 `GET /api/risk/limits` 硬风控阈值只读视图（runtime 覆盖后生效值）

待办清单文档已回写完成状态；openapi.json 重导，前端 `npm run gen:api` 后删兼容代码。

## 二、P4a · 设置三分区（handoff 3.6）

`/api/settings/{exchange,llm,notifications}` GET/PUT + `POST .../test`（全部 admin+）：

- 存储复用 runtime_config 的 system_settings + Fernet 加密；**任何 GET 永不回明文**（只回 ****尾4位），PUT 审计体只记键名
- 交易所测试连接返回权限清单 `{read, trade, withdraw}`，**withdraw=true 返回强警告**（适配层新增 `get_account_permissions`，用 spot get_account 的 canTrade/canWithdraw）
- LLM 分区含温度/超时/per-agent 模型分工（llm.agent_models）；test 用注入工厂真调一次 complete
- 通知 V1 存渠道开关+事件订阅（渠道 token 仍走 env，notifier 接线记遗留）

## 三、P4b · RBAC 四角色（handoff 3.7）

- UserRole 扩 owner/trader/viewer（legacy user→trader 别名），UserStatus 加 pending；role 列本就 String(20)，**无 schema 迁移**
- `require_permission(key)` 依赖工厂（`services/system/permissions.py`）：矩阵判定 fail-closed，未知键创建期 fail-fast；`require_admin` 兼容 owner
- 端点切换：orders precheck/下单、sltp、close-position/close-all → `trade.manual_order`（**trader 放行、viewer 403**）；pause/resume/resolve-breaker → `trade.engine_toggle`；settings/admin 仍 admin+
- owner 保护：不可通过接口创建/修改/授予 owner；admin 不能改自己；`POST /api/admin/users/{id}/approve` 批准 pending 用户
- **对照测试**（roadmap P4 验收）：5 角色 × 守卫端点参数化断言「矩阵判定 == 端点行为」；viewer 调全部交易接口 400004

## 四、P4c · 2FA TOTP 二段式（handoff 3.7）

- 新依赖 pyotp；users 表加 totp_secret（**Fernet 加密存储**）+ two_fa_enabled（迁移 `20260705_210401`）
- `POST /api/auth/2fa/setup`（secret+otpauth URI，verify 前不启用）/ `verify` / `disable`（需有效 code）
- 登录二段式：启用 2FA 的用户 login 只返回 `{requires_2fa, two_fa_token}`（短期 scope=2fa 票据，**不发 token 不落 cookie**）→ `POST /api/auth/2fa/login {two_fa_token, code}` 换正式 token + cookie；正式 token 冒充票据被 scope 校验拒绝（守卫测试）
- `/api/auth/me` 与登录返回带 `two_fa_enabled`（前端 fromWireUser 占位可换真值）

## 验证

- 全量回归 **668 passed + 2 skipped**（本轮起点 647），ruff 全绿，openapi.json 58 paths
- 关键安全用例：明文 Key 全链路无回显 / secret 加密入库断言 / viewer 403 矩阵对照 / 2FA 票据 scope 守卫

## commits

`33f69fd` 联调缺口收口 → `e0f81f3` settings 三分区 → `d9fcda5` RBAC → `d355f6d` 2FA

## 遗留

- 老板本机 dev 库欠 `make upgrade-db`（累计 3 个迁移：risk_events.decision_id / agent_pending_actions / users 2FA 列）
- 通知渠道 token 的 settings 化 + notifier 接线；owner 角色的 bootstrap 授予（当前 DEFAULT_ADMIN 是 admin）
- 下一期：**P5 策略实验室**（lab 域 + 影子执行调度 + 灰度自动回滚）→ **P6 绩效归因**（含联调缺口#5）
