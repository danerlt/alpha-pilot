# 2026-05-01 22:30 — `backend/src/shared/` 彻底迁移并删除

## 背景
`backend/src/shared/` 留有 7 个文件 (4 个 shim + 3 个真实实现), 共 108 处 import。本次将真实实现迁到 canonical 位置, shim 全部消除, `src/shared/` 整目录删除。

## 7 个文件最终去向

| 来源 | 去向 | 类型 |
|---|---|---|
| `shared/config.py` | 删 (shim, 替换为 `src.configs.app_configs`) | shim |
| `shared/config_diagnostics.py` | 删 (shim, 替换为 `src.configs.app_configs`) | shim |
| `shared/constants.py` | 删 (shim, 替换为 `src.common.constants`) | shim |
| `shared/db.py` | 删 (shim, 替换为 `src.db.engines` / `src.db.session`) | shim |
| `shared/datetime_utils.py` | `src/utils/datetime.py` | git mv |
| `shared/enums.py` | `src/common/enums.py` | git mv |
| `shared/runtime_config.py` | `src/services/system/runtime_config.py` | git mv (业务逻辑入 services) |

## 改动量
- 修了 **68 个文件**的 import (含 `migrations/env.py`、`scripts/create_tables.py`、所有 controllers / services / workers / models / tests)
- `Settings` 自动映射到 `AppConfig as Settings`, `get_base_settings` 映射到 `get_app_config as get_base_settings` (`app_configs.py` 已有 `get_base_settings = get_app_config` 别名兜底)
- `from src.shared.db import ...` 按符号拆成 `src.db.engines` / `src.db.session` 两行
- 裸标识符 `sync_engine` / `SessionLocal` 全部是局部赋值, 无人依赖 shim 的 `__getattr__`, 安全删除
- `runtime_config.py` 自身的 `from src.shared.enums` 同步改为 `from src.common.enums`
- `tests/conftest.py` / `src/utils/time.py` 中残留的注释也已更新

## 验证
```
ALPHAPILOT_SKIP_SECRET_VALIDATION=1 uv run pytest -q --tb=short
→ 439 passed, 2 skipped, 5 warnings in 87.49s
```
保持基线全绿。

## 后续
`tests/unit/shared/` 子目录暂保留 (test_secret_validation.py / test_datetime_utils.py 仍按旧分组), 内部 import 已更新; 后续如做测试目录重组可一并迁到 `tests/unit/configs/` / `tests/unit/utils/`。
