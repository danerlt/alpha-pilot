# 20260706 本地 e2e 验收报告（隔离环境全链路真跑）

## 环境

- 隔离栈：`alphapilot_e2e` 库（迁移至 head）/ Redis db7 / api :8801 / scheduler（策略循环 1 分钟）
- 临时随机密钥（仅进程 env，不落文件），不触碰老板任何真实配置
- 网络：Binance testnet/futures 公共端点可达（经本机代理）；**无真实 Binance/LLM Key**
  （交易所私有接口与真 LLM 决策链留老板按 `docs/testnet验收手册.md` 复验）

## 结果总览

**63 项断言：63 PASS**（首轮 56/59 + 补验 4/4，重叠 3 项为修复后复验）。
全量单测回归 697 passed + 2 skipped。

| 分区 | 验证项（全部 PASS） |
|------|--------------------|
| 认证 | 登录 / httpOnly cookie / cookie 免 Bearer / Bearer 兼容 / 匿名 401 |
| 2FA | setup→verify→二段式登录（第一段无 token 无 cookie）→disable，错误码拒绝 |
| RBAC | viewer 下单/设置 403、trader 放行、pending 拒登→批准→可登、不能改自己、矩阵下发 |
| 设置 | 交易所 Key 加密入库+脱敏（响应全文无明文）、health 凭证联动、假 Key 测试连接结构化失败、LLM/通知分区、**风控阈值 DB 覆盖生效** |
| 行情 | K线 50 根**真连 testnet**、24h ticker 真值、**USDT-M 资金费率/OI 真值**、自选列表持仓标记 |
| 下单 | 预检逐项、重复持仓拒、reduce-only 语义、**HALTED 全链路**（pause→state=HALTED→开仓拒/平仓行→resume 恢复）、假 Key 下单结构化失败不崩、非法 SL 拒 |
| 决策/绩效 | guard_verdict+价格字段、详情聚合、权益曲线、summary 对账、月度、归因、catchup 100 条 |
| Agent | SSE 流、历史落库、pending action 确认→**阈值即时生效（3→2）**、重复确认拒 |
| 实验室 | 提交→影子→门槛拒 promote→权限（viewer 只读/trader 不能 promote）→时间线 |
| WS | /ws 鉴权+无 token 拒、**/ws 实时推送**（pause→2 秒内收到 risk.state）、**/ws/market 真连 Binance 收到盘口** |
| 调度 | position_monitor 每 10s 稳定（440 条 position.updated 全部 published）、shuttle 搬运正常、假 Key 时 pipeline 异常被兜住进程不崩、lab_job 每轮评估 |
| 配置分层 | api/scheduler 启动日志均出现 `runtime settings loaded from DB`，且包含设置页写入的 BINANCE/LLM/NOTIFY 键 |

## e2e 抓到并已修复的三个真 bug（单测测不出的部署级问题）

| # | Bug | 影响 | 修复 |
|---|-----|------|------|
| 1 | spot testnet 行情 WS 域名错误（`testnet.binance.vision` 只有 REST，WS 握手 404） | /ws/market 在 testnet 永远连不上上游 | 改 `stream.testnet.binance.vision`，复验收到真实盘口 |
| 2 | `RedisStreamsBus(redis_url=)` 参数名错（应为 `url=`） | EventShuttle 线程启动即崩，事件永远停在 outbox | 修参数名，复验 shuttle 正常搬运+published 回填 |
| 3 | scheduler 构造 EventShuttle **漏传 pubsub** | 事件只进 Streams 不进 Pub/Sub → `/ws` 实时推送整条链路从未工作 | 注入 redis client，复验 pause 后 2 秒内 WS 收到 risk.state |

> 三处都在"scheduler 真进程装配"路径上——单测/集成用注入对象绕过了装配层，这正是本次 e2e 的价值。

## 已知边界（非缺陷，留老板复验）

1. **真实决策链**（LLM 真调用 + 账户同步 + 下单成交）需真实 testnet Key：假 Key 时
   `get_balance` 被 Binance 拒、LLM 回退 Mock 恒 HOLD——异常兜住、进程稳定（已验证）
2. 本机代理（127.0.0.1:10808）承载 Binance 流量，长跑曾见 WinError 10055（socket 缓冲耗尽）；
   服务器部署（直连或稳定代理）无此问题，本机 24h 观察时留意
3. 前端浏览器级走查建议随老板验收一起做（后端 API/WS 契约本轮已全部真跑）

## 修复 commit

`f9f1e3f` fix(events/market): e2e 验收修复三连

## 清理

e2e 进程已停、`alphapilot_e2e` 库与 Redis db7 已清理，未触碰任何真实数据。
