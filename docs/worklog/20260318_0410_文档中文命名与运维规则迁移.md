# 20260318 04:10 文档中文命名与运维规则迁移

## 做了什么

1. 将 docs 目录下现有英文文件名批量迁移为中文文件名。
2. 将 docs/worklog 下旧的 `2026-03-17-主题.md` 命名迁移为 `YYYYMMDD_HHMM_中文主题.md`。
3. 在 `agents/ops/AGENTS.md` 中加入文档治理职责与命名规则。
4. 在 `agents/planner/AGENTS.md`、`agents/alpha-pilot/AGENTS.md` 中同步补充文档规则。
5. 对仓库内可识别的旧文档路径引用做了同步替换。

## 命名策略

- docs 普通文档：统一使用中文文件名。
- docs/worklog：统一使用 `YYYYMMDD_HHMM_中文主题.md`。
- 对历史 worklog，由于原文件缺少分钟级时间信息，本次统一按同一日期顺序分配时间段，从 `0800` 递增到 `0829`，用于形成稳定且可排序的命名，不代表真实写作分钟时间。

## 风险与遗留

1. 文档内容内部若存在自然语言提及旧文件名，可能仍有少量未替换。
2. 当前仅完成文件级迁移与主要规则落地，后续新增文档仍需持续遵守该规范。
3. 如后面要把目录名也彻底中文化，需要单独做一轮目录迁移与引用修复。
