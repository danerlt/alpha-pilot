# 服务器部署 Runbook 阶段 0→5 执行记录（dev）

- 时间：2026-07-06
- 执行者：服务器 Claude Code（按 `docs/runbook-server-bootstrap.md` 逐阶段执行，阶段 6/7 按老板指示暂缓）

## 做了什么

| 阶段 | 动作 | 结果 |
|------|------|------|
| 0 前置检查 | Docker 29.3.0 / Compose v5.1.0 / git 2.43.7；2核 3.6G+8G swap；磁盘剩 12G | ✅ 通过 |
| 1 目录+中间件 | 均已存在（6-28 首次打通）：`ap-postgres`/`ap-redis` healthy 8 天、三库已建、`ap-shared` 在、Redis PONG | ✅ 验证通过，无需重做 |
| 2 dev env | `deploy/dev/envs/dev.env` 已存在（老板已填），全程未读取未回显 | ✅ 跳过创建 |
| 3 部署 dev | CI 已自动部署最新 dev `60ae398`（四 service 含 webapp:3004 全 Up）；补跑 4 项验证：compose ps ✅ / `:8001/health` ✅ / `APScheduler started` + `runtime settings loaded from DB` ✅（配置分层生效）/ 无 InsecureSecretError ✅ | ✅ 全过 |
| 4 nginx | `/etc/nginx/conf.d/openclaw.conf` 补 `/ap-dev-next` 两个 location 块（→127.0.0.1:3004；先备份 `openclaw.conf.bak-20260706`）；`nginx -t` + reload；域名侧 `/ap-dev/api/health` envelope ✅、`/ap-dev-next/` 200 ✅、`config.js` 注入 `apiBaseUrl=/ap-dev` ✅、SPA fallback `/login` 200 ✅ | ✅ 全过 |
| 5 业务配置 | 引导项：发现 DB 内 Binance testnet Key 已失效，待老板浏览器操作（见下） | ⏸ 卡老板操作 |

## 发现的问题

1. **Binance testnet Key 失效（阶段 5 阻塞项）**：策略链每 15 分钟周期均报
   `BinanceAPIException APIError(code=-2015): Invalid API-key, IP, or permissions`（`get_asset_balance` 处）。
   `alphapilot_dev.ai_decisions` 为 0 条（v3.7 重建后从未产生决策），非本次部署回归。
   处置：老板在 `https://www.danerlt.top/ap-dev-next/` 设置页换新 testnet Key →「测试连接」→ 等一个策略周期验证出决策。
2. **nginx 限流缺口（待老板决定）**：服务器现有 AlphaPilot 块为无限流简化版——全 nginx 无
   `limit_req_zone ap_login/ap_api`、ws 块无 `ap_log_no_args` log_format（模板
   `docker/nginx/alpha-pilot.conf` 里有，对应 post-Plan5 安全审计 H4/H5）。本次仅按最小增量补
   `/ap-dev-next`，未擅自给 prod/test 块加限流。

## 如何验证

- `curl https://www.danerlt.top/ap-dev/api/health` → success envelope（testnet，binance/llm configured=true）
- `curl -I https://www.danerlt.top/ap-dev-next/` → 200；`/ap-dev-next/config.js` → `apiBaseUrl:"/ap-dev"`
- `docker logs ap-dev-scheduler-1 | grep "runtime settings loaded from DB"` → 有（配置分层证据）

## 对应变更

- 服务器侧：`/etc/nginx/conf.d/openclaw.conf`（+ 备份），不进 git
- 仓库侧：仅本 worklog
