# 20260318 04:12 运行产物模板与角色脚手架补齐

## 做了什么

1. 新增 `runs/README.md`，定义 run artifacts 的目标、目录结构和阶段规则。
2. 新增 `runs/templates/feature/` 与 `runs/templates/bug/` 两套模板。
3. 为 feature 与 bug 任务分别补齐：任务说明、规划交接、开发交接、运维总结模板。
4. 在 `agents/planner/AGENTS.md`、`agents/developer/AGENTS.md`、`agents/ops/AGENTS.md`、`agents/alpha-pilot/AGENTS.md` 中补充运行产物规则。
5. 明确各角色对 `runs/<task-id>/` 目录与交接文件的责任边界。

## 为什么这样做

之前虽然已经有流程说明，但缺少真正可落地的 run artifacts 约束。补齐模板后：
- planner 有固定交付入口
- developer 有固定测试与实现回填入口
- ops 有固定验收与部署总结入口
- alpha-pilot 有固定阶段流转检查点

## 验证

- 已检查 `runs/` 目录结构和模板文件存在。
- 已检查四个 agent 的 AGENTS.md 中包含运行产物规则段落。

## 后续建议

下一步可以继续把 `runs/templates/` 与实际自动化入口绑定，例如：
- 新任务自动初始化 run 目录
- 阶段切换时自动校验交接文件字段
- 结合 alpha-pilot 调度逻辑自动生成阶段播报摘要
