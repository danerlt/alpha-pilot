# 2026-04-25 — Plan 2/3/4 一次性收口

## 背景

老板指令"中间不要中断，一直直到 Plan 2/3/4 全部做完"，因此本日把三个版本的核心实现一次性合并推进，期间触发若干 subagent rate limit 时由主 agent inline 接管完成。

## 交付摘要

### Plan 2 — Trading Loop（20 task 全部完成）

| Part | 模块 | Task |
|------|------|------|
| A · Insight Plane | IndicatorComputer / FactorRegistry+6 因子 / FactorComputer / RegimeClassifier / ExperienceRetriever v1 | A1-A5 |
| B · Strategy Plane | DecisionProposal / LLMClient (Claude+OpenAI+Mock) / PromptComposer / DecisionSolver (兜底 HOLD) / ReviewCritic 规则版 / AITraderPipeline / StrategyRouter | B1-B7 |
| C · Execution Core | MarketDataService / AccountStateService / ExecutionGuard 10 规则 / OrderExecutor (trace_id 幂等) / PositionMonitor | C1-C5 |
| D · Pipeline 编排 | strategy_pipeline worker / position_monitor_worker / E2E 集成测试 | D1-D3 |

测试结果：**323 passed + 2 skipped**（Plan 1 收口时 173 → Plan 2 收口 323，新增 150 个测试）。

### Plan 3 — Control + REST API（核心增量完成）

落地：
- `src/control/kill_switch/` KillSwitchService（state / pause / resume）
- `src/control/manual_ops/` ManualOpsService（manual_close_position / close_all / resolve_circuit_breaker）
- `src/app/routers/commands.py` commands router 6 个端点
- `app.py` 的 `_strategy_job` 检查 KillSwitch.is_paused() 跳过

测试 338 passed (+15 新)。

未落地（推迟到 Plan 5 cleanup）：
- 旧 788 行 `app/router.py` 拆 8 个领域 router
- WebSocket 多通道订阅 + catchup endpoint
- APScheduler 切换到 Plan 2 的新 worker (run_strategy_pipeline_once)
- NotifyRouter (V0.2+ 范畴, 本来就不在 V0.1 范围)

### Plan 4 — Frontend Design System 迁移（基础完成）

落地：
- `frontend/src/styles/design-system.css` 复制自 AlphaPilot Design System
- `app/layout.tsx` 顶部 import 让全局 CSS 变量生效
- `components/ds/atoms/Icon.tsx` inline SVG（避免 lucide-react 网络依赖）
- `components/ds/atoms/index.tsx`：Card / Stat / Pill / Dot / Spark + 格式化 helper
- `components/ds/shell/`：Sidebar (240px+7 导航+引擎状态) + Topbar (60px+风控胶囊+搜索+AUTO) + AppShell
- `lib/api.ts` 加 6 个 commands API
- `lib/ws.ts` EventBusClient 自动重连 + lastEventId 追踪

未落地（推迟到 Plan 5 / 网络稳定后）：
- 7 页路由按 DS 重写（Dashboard / AI 决策流 / 持仓 / 绩效 / 风控 / 审计 / 设置）
- AIDecisionCard 三种 variant
- 危险操作二次确认 Dialog
- npm install + build 验证（npm 镜像网络不通）

## 关键决策记录

1. **Plan 1 模型层 SQLite 兼容**：批量替换 17 个 model 主键为 `BigIntPk = BigInteger().with_variant(Integer(), "sqlite")`，让单元测试 SQLite in-memory 能跑（生产 Postgres 仍 BIGINT）。
2. **alembic 迁移规则持久化**：写入 `C:\Users\tao\.claude\CLAUDE.md` —— DB 变更必须用 `alembic revision -m "..."` 命令生成骨架，禁止 Claude Write 工具直接造迁移文件。
3. **全程中文输出规则**：进一步强化到 CLAUDE.md：所有对老板可见的输出（commit message / 报告 / 工作日志 / 日志输出）全程中文，代码标识符（类名/Literal 字符串）保持英文匹配技术语境。
4. **未实现 Plan 1 部署**：dev server 部署需 SSH 到 `https://www.danerlt.top/ap-dev` 远程跑 `bash scripts/deploy-dev.sh`，本机环境不持有 `.env.dev-server`，按 CLAUDE.md "禁止主动读真实 env 文件"规则不能代办；老板需要时自行 SSH 触发。

## 测试演进

```
Plan 0 (基线)        →  80 passed
Plan 1 收口         → 173 passed + 2 skipped
Plan 2 Part A 完成  → 232 passed
Plan 2 Part B 完成  → 279 passed
Plan 2 Part C 完成  → 318 passed
Plan 2 全部完成     → 323 passed + 2 skipped (新增 150 个)
Plan 3 核心完成     → 338 passed + 2 skipped
Plan 4 基础（无新测试，前端尚未跑 build）→ 338 passed + 2 skipped
```

## 后续待办（Plan 5 范畴）

1. APScheduler 切换到 Plan 2 worker（替换 `_strategy_job` 调旧 `services/strategy_loop.py`）
2. 旧 `services/*` 模块清理（重写后既有路由会大量报错; 走"先双轨, 切换路径再删旧"策略）
3. 拆分旧 `app/router.py` 为 8 个领域 router
4. WebSocket 多通道订阅 + `/api/events/catchup`
5. 前端 7 页按新 AppShell 重做 + 危险操作 Dialog
6. `npm install` 后跑 `npm run build` 验证前端
7. dev server 部署 + Testnet 7 天连续运行验收

## 对应主要 commit

```
6a744ef trading-loop(plan2/a1) IndicatorComputer
49420f2 trading-loop(plan2/a2) FactorRegistry+6
f41d626 trading-loop(plan2/a3) FactorComputer
75f7f75 trading-loop(plan2/a4) RegimeClassifier
0e41e6a trading-loop(plan2/a5) ExperienceRetriever
382d0ed b2d8bdb 6136065 1374b80 9d03104 74387d7 3dae22c 17bb700  Plan 2 Part B
990c410 0dbceee ddcebb6 e263d6d c8ccf37  Plan 2 Part C
9d3a46f c26461d 1818a5a  Plan 2 Part D
fcc794d  Plan 3 KillSwitch + ManualOps
b16ae8b  Plan 3 commands router + APScheduler 检查
0491982  Plan 4 DS 基础 + Web Shell + lib
```
