# Auth & Admin Implementation Plan

## Phase A — Auth Foundation
- [x] Add `users` table + migration
- [x] Add password hash + JWT service
- [x] Add register/login/me endpoints
- [x] Add auth API tests

## Phase B — Authorization Foundation
- [x] Add `require_admin()` dependency
- [x] Restrict runtime config updates to admins
- [x] Add admin permission tests

## Phase C — Admin Data Layer
- [x] Add `symbol_configs` table
- [x] Add `audit_logs` table
- [x] Add symbol config list/create/update APIs
- [x] Add symbol admin API tests

## Phase D — Admin User Management
- [x] Add admin list users API
- [x] Add admin update user role/status API
- [x] Add audit logs for user management changes
- [x] Add tests for admin user management

## Phase E — Frontend Auth & Admin Entry
- [x] Add login page（`frontend/src/app/login/page.tsx`）
- [x] Add register page（`frontend/src/app/register/page.tsx`；按安全审计 C5 公开注册有意禁用，页面显示"联系管理员"提示）
- [x] Add auth state storage（`frontend/src/lib/auth.ts` + `components/auth-provider.tsx`，localStorage 会话 + /auth/me 刷新）
- [x] Add header/admin entry（`frontend/src/components/app-shell.tsx`，登录态 + admin 入口按角色显隐）

## Phase F — Frontend Admin Views
- [x] Add symbol management page（`frontend/src/app/admin/currencies/page.tsx`）
- [x] Add user management page（`frontend/src/app/admin/users/page.tsx`）
- [x] Connect admin UI to backend APIs（apiRequest + buildAuthHeaders 接真实后端，envelope+JWT）
- [x] Keep mobile-first responsive layout（globals.css 960px 断点 + adminMobileOnly/adminDesktopOnly 卡片切换）

## Phase G — Product Architecture Follow-ups
- [x] Add docs/总体架构.md to describe control plane + execution core + future strategy intelligence layers（[`docs/总体架构.md`](../总体架构.md)）
- [x] Add attribution analytics plan/spec skeleton（已在 [v1 spec](../superpowers/specs/2026-04-21-alphapilot-v1-system-design.md) §5.4/5.5 完成设计，总体架构.md §5 跟踪）
- [x] Add AI Trader / Program Trader split as a tracked roadmap item（v1 spec §4.2/4.3/4.5 + 总体架构.md §5）
- [x] Add factor intelligence/factor library roadmap skeleton（v1 spec §5.1/5.2 因子层 + 总体架构.md §5）

> Phase G 的 attribution / 双模式 / 因子三项：内容已由 v1 系统设计 spec 完整覆盖，本轮以 `docs/总体架构.md` §5 做统一路线跟踪，不再另起碎片化骨架文件（见该文 §5 末注）。

## Verification Rule
For each phase chunk:
1. Update docs/spec or plan if scope changes
2. Implement in small verified steps
3. Run relevant tests/build
4. Commit + push
5. Deploy dev server
6. Append worklog entry
