# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Environment files —— 任何真实 env 文件都不提交; envs/ 整个目录是老板本地存档.
# 必须同时覆盖三种命名: `.env`, `.env.*` (如 .env.local), `*.env` (如 dev.env).
# 唯一模板 example.env 通过反向规则保留.
.env
.env.*
*.env
!example.env
envs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/next-env.d.ts

# OS
.DS_Store
Thumbs.db

# log
logs/
*.log

# Local SQLite fallback DB (e.g. if alembic runs without DATABASE_URL)
backend/*.db
backend/*.sqlite
backend/*.sqlite3

# OpenClaw / agent runtime local files
.openclaw/
AGENTS.md
SOUL.md
USER.md
TOOLS.md
IDENTITY.md
HEARTBEAT.md
agents/
config/agent_runners.json
memory/
runs/
skills/
# 仅忽略根级 agent runtime 的 workflows/, 放行 .github/workflows/ (CI)
/workflows/
